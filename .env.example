NEXT_PUBLIC_PROJECT_ID=""

# Set to "true" to disable market/boost windows subgraph (avoids "Query data cannot be undefined" when subgraph is down or for local-only builds)
# NEXT_PUBLIC_DISABLE_MARKET_BOOST_SUBGRAPH=true