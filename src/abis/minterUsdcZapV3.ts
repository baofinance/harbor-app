/**
 * Minter USDC / ERC-20 base zap — v4 unified ABI (same selectors as ETH zap where shared).
 * Export name kept as MINTER_USDC_ZAP_V3_ABI for stable imports.
 */
export { MINTER_ZAP_V4_ABI as MINTER_USDC_ZAP_V3_ABI } from "./minterZapV4Abi";
