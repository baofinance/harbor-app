/**
 * Route-scoped display helpers for the Anchor index.
 * Re-export from `utils/anchor` until Anchor-specific formatting diverges
 * (then implement here and update imports incrementally).
 */
export { formatCompactUSD, formatAPR, formatRatio } from "./anchor";
